interface Theme {
  colors: {
    primary: string;
    background: string;
  };
}

const theme: Theme = {
  colors: {
    primary: '#173355',
    background: '#FEFEFE',
  },
};

export default theme;
